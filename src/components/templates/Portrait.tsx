// not sure if this architecture makes sense yet...

import { WidgetProps } from "../types";

const Portrait = (props: WidgetProps) => {
  return (
    <div>
      <p>portrait view</p>
    </div>
  );
};

export default Portrait;
