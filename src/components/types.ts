export type SizeOptions = {
  minW?: number;
  w?: number;
  maxW?: number;
  minH?: number;
  h?: number;
  maxH?: number;
};

export type WidgetMetadata = {
  fileName: string;
  sizeOptions: SizeOptions;
};

export type WidgetProps = {};
